{
  "name": "wisipoo-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for my React component library and playground",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build:lib": "pnpm --filter my-awesome-lib build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.7.1"
  }
}